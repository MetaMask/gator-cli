{
  "name": "permissions-cli",
  "version": "0.1.0",
  "description": "ERC-7710 Delegation CLI — grant, redeem, and revoke permissions on MetaMask Smart Accounts",
  "type": "module",
  "bin": {
    "permissions-cli": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@metamask/smart-accounts-kit": "^0.3.0",
    "commander": "^12.1.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.5.1",
    "tsx": "^4.19.0",
    "typescript": "~5.7.3",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": "^20 || ^22 || >=24"
  },
  "private": true
}
